# Task: fix classify

`broken.py` has a `classify(score)` that should return `"pass"` when the score is
**60 or above** (60 counts as a pass) and `"fail"` otherwise. The boundary is
wrong. Fix `broken.py`. Do not change the signature.
