# Task: fix last_n

`broken.py` has a `last_n(items, n)` that should return the last `n` items of a
list, in order (`last_n([1,2,3,4,5], 2)` → `[4, 5]`, `last_n(x, 0)` → `[]`). It
is off. Fix `broken.py` so the behaviour is correct. Do not change the signature.
