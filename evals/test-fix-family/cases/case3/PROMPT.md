# Task: fix running_max

`broken.py` has a `running_max(nums)` that should return a list where element `i`
is the max of `nums[0..i]` (`running_max([3,1,4,1,5])` → `[3,3,4,4,5]`). It gives
wrong answers on some inputs. Fix `broken.py`. Do not change the signature.
