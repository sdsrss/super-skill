# acme-tool

A tiny demo tool.

## Usage

Install the package, then run the command below. The `--fast` flag skips the
warm-up pass and is safe for repeated runs.

```bash
acme-tool run --fast
```

## License

MIT.
