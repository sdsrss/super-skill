# Task: mechanical rename

Rename the function `calc` to `calculate_stats` everywhere in this small
module (definition, internal uses, tests, imports). No behaviour change; the
test suite must stay green. Work inside this directory.
